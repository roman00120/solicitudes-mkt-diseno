@props(['name' => 'drawer', 'title' => 'Panel'])
<div x-data="{ open: false }" x-on:open-drawer.window="if ($event.detail === '{{ $name }}') open = true" x-on:keydown.escape.window="open = false" x-show="open" x-cloak class="fixed inset-0 z-[var(--z-drawer)]" role="dialog" aria-modal="true" aria-labelledby="{{ $name }}-title"><div class="absolute inset-0 bg-black/60" @click="open = false"></div><aside class="absolute right-0 top-0 h-full w-full max-w-md overflow-y-auto border-l border-[var(--color-border-default)] bg-[var(--color-surface-elevated)] p-6 shadow-[var(--shadow-lg)]"><div class="flex items-center justify-between"><h2 id="{{ $name }}-title" class="text-lg font-semibold">{{ $title }}</h2><x-ui.icon-button label="Cerrar" icon="x" @click="open = false" /></div><div class="mt-6">{{ $slot }}</div></aside></div>
