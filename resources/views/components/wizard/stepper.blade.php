@props(['steps' => [], 'current' => 2])
<ol {{ $attributes->merge(['class' => 'flex items-start gap-2 overflow-x-auto pb-2']) }} aria-label="Progreso del formulario">@foreach($steps as $index => $step)<li class="flex min-w-28 flex-1 items-center gap-2"><span class="inline-flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-xs font-bold {{ $index < $current ? 'bg-green-500 text-slate-950' : ($index === $current ? 'bg-[var(--color-action-primary)] text-white ring-4 ring-[var(--color-focus-ring)]' : 'bg-[var(--color-surface-interactive)] text-[var(--color-text-tertiary)]') }}">@if($index < $current)<i data-lucide="check" class="h-4 w-4" aria-hidden="true"></i>@else{{ $index + 1 }}@endif</span><span class="text-xs {{ $index === $current ? 'font-semibold text-white' : 'text-[var(--color-text-tertiary)]' }}">{{ $step }}</span>@if(!$loop->last)<span class="h-px flex-1 bg-[var(--color-border-default)]"></span>@endif</li>@endforeach</ol>
