@props(['name' => 'Andrea Martínez', 'role' => 'Marketing', 'time' => 'Hace 12 min', 'internal' => false, 'edited' => false])
<article {{ $attributes->merge(['class' => 'flex gap-3 rounded-[var(--radius-md)] border p-4 ' . ($internal ? 'border-amber-500/30 bg-amber-500/5' : 'border-[var(--color-border-subtle)] bg-[var(--color-surface-default)]')]) }}><x-ui.avatar :name="$name" size="sm" /><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2"><span class="text-sm font-semibold">{{ $name }}</span><span class="text-xs text-[var(--color-text-tertiary)]">{{ $role }} · {{ $time }}</span>@if($edited)<span class="text-xs text-[var(--color-text-tertiary)]">Editado</span>@endif</div>@if($internal)<x-ui.badge variant="warning" size="sm" icon="lock-keyhole">Nota interna</x-ui.badge>@endif<div class="mt-2 text-sm leading-6 text-[var(--color-text-secondary)]">{{ $slot }}</div></div></article>
