@props(['item'])
<article class="rounded-[var(--radius-md)] border border-[var(--color-border-subtle)] bg-[var(--color-surface-default)] p-4"><div class="flex flex-wrap items-start justify-between gap-3"><div><p class="text-xs font-semibold text-[var(--color-text-tertiary)]">{{ $item['id'] }}</p><h3 class="mt-1 font-semibold">{{ $item['title'] }}</h3></div><x-request.priority-badge :priority="$item['priority']" /></div><div class="mt-3 flex flex-wrap items-center gap-2"><x-request.status-badge :status="$item['status']" /><x-request.service-badge :service="$item['service']" /></div><p class="mt-3 text-sm text-[var(--color-text-secondary)]">{{ match($item['status']) { 'En espera de información' => 'El equipo solicitó información adicional.', 'En revisión de Marketing' => 'Hay un entregable listo para revisión.', default => 'Hay correcciones pendientes de confirmar.' } }}</p><div class="mt-4 flex items-center justify-between gap-3">@if(!empty($item['due_at']))<time class="text-xs text-[var(--color-text-tertiary)]" datetime="{{ $item['due_at']->toIso8601String() }}">{{ $item['due_at']->locale('es')->translatedFormat('d M Y') }}</time>@else<span class="text-xs text-[var(--color-text-tertiary)]">Sin fecha</span>@endif<a href="{{ route('app.requests.show', $item['id']) }}" class="inline-flex min-h-11 items-center gap-2 text-sm font-semibold text-[var(--color-action-primary)] hover:text-white">{{ $item['status'] === 'En revisión de Marketing' ? 'Revisar' : 'Responder' }}<i data-lucide="arrow-right" class="h-4 w-4" aria-hidden="true"></i></a></div></article>
