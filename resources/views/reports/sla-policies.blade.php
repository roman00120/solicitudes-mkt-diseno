@extends('layouts.admin') @section('title','Políticas SLA') @section('header','Políticas SLA') @section('content')<h1 class="text-2xl font-bold">Configuración de SLA</h1><form method="POST" action="{{ route('admin.reports.sla.policies.store') }}" class="mt-6 grid gap-3 rounded border border-white/10 bg-[var(--color-bg-secondary)] p-5 sm:grid-cols-2 lg:grid-cols-4">@csrf<input required name="name" placeholder="Nombre" class="min-h-11 rounded bg-black/20 px-3"><select name="metric" class="min-h-11 rounded bg-black/20 px-3"><option value="initial_response">Respuesta inicial</option><option value="assignment">Asignación</option><option value="delivery">Entrega</option><option value="internal_review">Revisión interna</option><option value="corrections">Correcciones</option></select><select name="service" class="min-h-11 rounded bg-black/20 px-3"><option value="">Global</option><option value="design">Diseño</option><option value="video">Video</option><option value="render">Render</option></select><input required type="number" min="1" name="target_minutes" placeholder="Minutos objetivo" class="min-h-11 rounded bg-black/20 px-3"><input required type="number" min="1" max="99" name="warning_threshold_percent" value="80" placeholder="Umbral %" class="min-h-11 rounded bg-black/20 px-3"><input required type="date" name="effective_from" value="{{ today()->toDateString() }}" class="min-h-11 rounded bg-black/20 px-3"><button class="min-h-11 rounded bg-red-600 px-4">Crear política</button></form><div class="mt-6 overflow-x-auto rounded border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="p-3">Nombre</th><th class="p-3">Métrica</th><th class="p-3">Servicio</th><th class="p-3">Objetivo</th><th class="p-3">Estado</th></tr></thead><tbody>@foreach($policies as $policy)<tr class="border-t border-white/10"><td class="p-3">{{ $policy->name }}</td><td class="p-3">{{ $policy->metric }}</td><td class="p-3">{{ $policy->service ?? 'Global' }}</td><td class="p-3">{{ $policy->target_minutes }} min</td><td class="p-3">{{ $policy->is_active ? 'Activa' : 'Inactiva' }} <form class="inline" method="POST" action="{{ route('admin.reports.sla.policies.toggle',$policy) }}">@csrf<button class="ml-2 underline">Cambiar</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $policies->links() }}</div>@endsection
