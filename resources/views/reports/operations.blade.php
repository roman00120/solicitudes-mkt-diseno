@extends($layout) @section('title',$title) @section('header',$header)
@section('content')<div><p class="text-sm text-gray-400">Reportes / Operación</p><h1 class="text-2xl font-bold">{{ $title }}</h1><p class="mt-1 text-xs text-gray-400">Estado actual; no es una evaluación de desempeño individual.</p></div>@include('reports.partials.filters')<div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($report['statuses'] as $status=>$total)<x-report.metric-card :label="str_replace('_',' ',$status)" :value="$total" /></div>@endforeach<div class="mt-6 grid gap-6 lg:grid-cols-2"><section class="rounded border border-white/10 bg-[var(--color-bg-secondary)] p-4"><h2 class="font-semibold">Riesgos operativos</h2><dl class="mt-4 space-y-3 text-sm"><div class="flex justify-between"><dt>Sin asignar</dt><dd>{{ $report['unassigned'] }}</dd></div><div class="flex justify-between"><dt>Próximas a vencer</dt><dd>{{ $report['soon_due'] }}</dd></div><div class="flex justify-between"><dt>Vencidas</dt><dd>{{ $report['overdue'] }}</dd></div></dl></section><section class="rounded border border-white/10 bg-[var(--color-bg-secondary)] p-4"><h2 class="font-semibold">Carga actual</h2><table class="mt-3 w-full text-sm"><tbody>@forelse($report['workload'] as $row)<tr class="border-t border-white/10"><th class="p-2 text-left">{{ $row['name'] }}</th><td class="p-2 text-right">{{ $row['total'] }}</td></tr>@empty<tr><td class="p-3 text-gray-400">No hay carga asignada.</td></tr>@endforelse</tbody></table></section></div>@endsection
