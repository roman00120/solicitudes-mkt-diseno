@extends('layouts.admin') @section('title','Vistas guardadas') @section('header','Vistas guardadas') @section('content')<h1 class="text-2xl font-bold">Vistas guardadas</h1><form method="POST" action="{{ route('admin.reports.saved-views.store') }}" class="mt-6 flex flex-wrap gap-3">@csrf<input name="name" required placeholder="Nombre de la vista" class="min-h-11 rounded bg-white/10 px-3"><select name="report" class="min-h-11 rounded bg-white/10 px-3"><option value="executive">Ejecutivo</option><option value="operations">Operación</option><option value="requests">Solicitudes</option></select><button class="min-h-11 rounded bg-red-600 px-4">Guardar filtros actuales</button></form><div class="mt-6 space-y-3">@forelse($views as $view)<div class="flex justify-between rounded border border-white/10 p-4"><span>{{ $view->name }} <small class="text-gray-400">({{ $view->report }})</small></span><form method="POST" action="{{ route('admin.reports.saved-views.destroy',$view) }}">@csrf @method('DELETE')<button class="underline">Eliminar</button></form></div>@empty<p class="text-gray-400">Aún no tienes vistas guardadas.</p>@endforelse</div>@endsection
