@extends($layout) @section('title',$title) @section('header',$header)
@section('content')<h1 class="text-2xl font-bold">{{ $title }}</h1>@include('reports.partials.filters')<div class="mt-6 overflow-x-auto rounded border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="p-3">Folio</th><th class="p-3">Título</th><th class="p-3">Servicio</th><th class="p-3">Estado</th><th class="p-3">Enviada</th></tr></thead><tbody>@forelse($requests as $request)<tr class="border-t border-white/10"><td class="p-3">{{ $request->folio }}</td><td class="p-3">{{ $request->title }}</td><td class="p-3">{{ $request->service->value }}</td><td class="p-3">{{ $request->status->value }}</td><td class="p-3">{{ $request->submitted_at?->format('Y-m-d H:i') ?? '—' }}</td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-gray-400">No hay datos para este periodo.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $requests->links() }}</div>@endsection
