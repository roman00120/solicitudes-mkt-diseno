@extends('layouts.creative') @section('title','Carga del equipo') @section('header','Carga del equipo')
@section('content')<div class="mx-auto max-w-6xl space-y-5"><h1 class="text-2xl font-bold">Carga del equipo</h1><p class="text-sm text-slate-400">Conteos de solicitudes activas; no representa horas ni rendimiento.</p>@if(empty($workload))<div class="rounded border border-dashed border-slate-700 p-10 text-center text-sm text-slate-400">No hay solicitudes activas.</div>@else<div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($workload as $item)<article class="rounded border border-slate-700 bg-slate-900 p-5"><div class="flex justify-between"><h2 class="font-semibold">{{ $item['user'] }}</h2><span class="text-sm {{ $item['load'] === 'Alta' ? 'text-red-300' : 'text-slate-300' }}">{{ $item['load'] }}</span></div><strong class="mt-4 block text-3xl">{{ $item['total'] }}</strong><p class="text-xs text-slate-400">solicitudes activas</p><dl class="mt-4 grid grid-cols-2 gap-2 text-xs text-slate-400"><div>En proceso <b class="text-white">{{ $item['in_progress'] }}</b></div><div>En espera <b class="text-white">{{ $item['waiting'] }}</b></div><div>Revisión <b class="text-white">{{ $item['review'] }}</b></div><div>Vencidas <b class="text-white">{{ $item['overdue'] }}</b></div></dl></article>@endforeach</div>@endif</div>@endsection
