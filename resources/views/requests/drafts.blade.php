@extends('layouts.app')
@section('title', 'Borradores')
@section('header', 'Borradores')
@section('content')
<div class="mx-auto max-w-5xl space-y-6"><header><p class="ds-kicker">Mis avances</p><h1 class="mt-2 text-2xl font-bold">Borradores</h1><p class="mt-2 text-sm text-[var(--color-text-secondary)]">Retoma una solicitud sin enviar desde el último paso guardado.</p></header><div class="space-y-3">@forelse($drafts as $draft)<article class="flex flex-col gap-4 rounded-[var(--radius-card)] border border-[var(--color-border-subtle)] bg-[var(--color-surface-default)] p-5 sm:flex-row sm:items-center"><div class="min-w-0 flex-1"><p class="text-xs font-semibold text-[var(--color-text-tertiary)]">{{ $draft->folio }}</p><h2 class="mt-1 truncate font-semibold">{{ $draft->title ?: 'Sin título' }}</h2><p class="mt-2 text-sm text-[var(--color-text-secondary)]">{{ $draft->service->label() }} · Paso {{ $draft->current_step }} · Actualizado {{ $draft->updated_at->locale('es')->diffForHumans() }}</p></div><a href="{{ route('app.requests.drafts.edit', $draft) }}" class="inline-flex min-h-11 items-center justify-center gap-2 rounded-[var(--radius-md)] bg-[var(--color-action-primary)] px-4 text-sm font-semibold">Retomar <i data-lucide="arrow-right" class="h-4 w-4" aria-hidden="true"></i></a></article>@empty<x-ui.empty-state title="No tienes borradores" description="Tus avances aparecerán aquí cuando guardes una solicitud." icon="file-edit"><a href="{{ route('app.requests.create') }}" class="inline-flex min-h-11 items-center gap-2 rounded-[var(--radius-md)] bg-[var(--color-action-primary)] px-4 text-sm font-semibold">Crear solicitud</a></x-ui.empty-state>@endforelse</div></div>
@endsection
