@extends('layouts.app') @section('title','Entregables') @section('header','Entregables')
@section('content')<div class="mx-auto max-w-5xl space-y-5"><a class="text-sm underline" href="{{ route('app.requests.show',$creativeRequest) }}">← Solicitud</a><h1 class="text-2xl font-bold">Entregables</h1>@forelse($deliverables as $deliverable)<a href="{{ route('app.requests.deliverables.show',[$creativeRequest,$deliverable]) }}" class="block rounded border border-slate-700 bg-slate-900 p-5"><h2 class="font-semibold">{{ $deliverable->title }}</h2><p class="mt-2 text-sm text-slate-400">{{ $deliverable->status->label() }} · versión {{ $deliverable->currentVersion?->version_number ?? '—' }}</p></a>@empty<div class="rounded border border-dashed border-slate-700 p-10 text-center"><h2 class="font-semibold">El equipo creativo está preparando tu solicitud</h2><p class="mt-2 text-sm text-slate-400">Los archivos aparecerán aquí cuando estén listos para revisión.</p></div>@endforelse</div>@endsection
