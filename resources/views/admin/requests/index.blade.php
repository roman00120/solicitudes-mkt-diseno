@extends('layouts.admin') @section('title','Solicitudes') @section('header','Solicitudes globales')
@section('content')<div class="flex justify-between"><div><h1 class="text-2xl font-bold">Solicitudes globales</h1><p class="text-sm text-gray-400">Consulta operativa de todas las áreas.</p></div><a class="rounded border border-white/20 px-4 py-3 text-sm" href="{{ route('admin.exports.requests') }}">Exportar CSV</a></div><form class="mt-6 flex flex-wrap gap-3" method="GET"><input name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Buscar" class="rounded bg-white/10 px-3 py-3"><input name="status" value="{{ $filters['status'] ?? '' }}" placeholder="Estado" class="rounded bg-white/10 px-3 py-3"><button class="rounded bg-white/10 px-4 py-3">Filtrar</button></form><div class="mt-6 overflow-x-auto rounded border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="p-3">Solicitud</th><th class="p-3">Solicitante</th><th class="p-3">Servicio</th><th class="p-3">Estado</th><th class="p-3"></th></tr></thead><tbody>@forelse($requests as $request)<tr class="border-t border-white/10"><td class="p-3"><a class="underline" href="{{ route('admin.requests.show',$request) }}">{{ $request->title }}</a></td><td class="p-3">{{ $request->requester?->name }}</td><td class="p-3">{{ $request->service }}</td><td class="p-3">{{ $request->status }}</td><td class="p-3">{{ optional($request->required_date)->format('Y-m-d') }}</td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-gray-400">Sin solicitudes.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $requests->links() }}</div>@endsection
