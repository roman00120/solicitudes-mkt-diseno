@extends('layouts.admin') @section('title','Detalle de solicitud') @section('header','Detalle de solicitud') @section('content')<div class="flex flex-wrap items-center justify-between gap-3"><a class="text-sm underline" href="{{ route('admin.requests.index') }}">← Volver</a><form method="POST" action="{{ route('admin.requests.destroy', $requestModel) }}" onsubmit="return confirm('¿Eliminar esta solicitud en cualquier estado? Esta acción la quitará del listado operativo.')">@csrf @method('DELETE')<button class="rounded border border-red-500/60 bg-red-500/10 px-4 py-2 text-sm font-bold text-red-300 hover:bg-red-500/20">Eliminar solicitud</button></form></div><h1 class="mt-4 text-2xl font-bold">{{ $requestModel->title }}</h1><dl class="mt-6 grid gap-4 sm:grid-cols-2"><div class="rounded border border-white/10 p-4"><dt class="text-xs text-gray-400">Estado</dt><dd class="mt-1 font-semibold">{{ $requestModel->status }}</dd></div><div class="rounded border border-white/10 p-4"><dt class="text-xs text-gray-400">Solicitante</dt><dd class="mt-1">{{ $requestModel->requester?->name }}</dd></div><div class="rounded border border-white/10 p-4 sm:col-span-2"><dt class="text-xs text-gray-400">Descripción</dt><dd class="mt-1 whitespace-pre-wrap">{{ $requestModel->description }}</dd></div></dl>@endsection
