@extends('layouts.admin') @section('title','Auditoría') @section('header','Auditoría') @section('content')<h1 class="text-2xl font-bold">Registro de auditoría</h1><p class="mt-2 text-sm text-gray-400">Registro inmutable de acciones administrativas; secretos excluidos.</p><div class="mt-6 overflow-x-auto rounded border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="p-3">Fecha</th><th class="p-3">Actor</th><th class="p-3">Acción</th><th class="p-3">Objetivo</th></tr></thead><tbody>@forelse($logs as $log)<tr class="border-t border-white/10"><td class="p-3">{{ $log->created_at }}</td><td class="p-3">{{ $log->actor?->email ?? 'sistema' }}</td><td class="p-3">{{ $log->action }}</td><td class="p-3">{{ $log->targetUser?->email ?? $log->auditable_type }}</td></tr>@empty<tr><td colspan="4" class="p-6 text-center text-gray-400">Sin eventos.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $logs->links() }}</div>@endsection
