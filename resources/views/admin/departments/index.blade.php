@extends('layouts.admin') @section('title','Departamentos') @section('header','Departamentos')
@section('content')<div class="flex justify-between"><h1 class="text-2xl font-bold">Departamentos</h1><a class="rounded bg-red-600 px-4 py-3 text-sm" href="{{ route('admin.departments.create') }}">Nuevo departamento</a></div><div class="mt-6 overflow-x-auto rounded border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="p-3">Nombre</th><th class="p-3">Código</th><th class="p-3">Usuarios</th><th class="p-3">Estado</th><th class="p-3"></th></tr></thead><tbody>@foreach($departments as $department)<tr class="border-t border-white/10"><td class="p-3">{{ $department->name }}</td><td class="p-3">{{ $department->code }}</td><td class="p-3">{{ $department->users_count }}</td><td class="p-3">{{ $department->is_active ? 'Activo' : 'Inactivo' }}</td><td class="p-3 text-right"><a class="mr-3 underline" href="{{ route('admin.departments.edit',$department) }}">Editar</a><form class="inline" method="POST" action="{{ route($department->is_active ? 'admin.departments.deactivate' : 'admin.departments.activate',$department) }}">@csrf<button class="underline">{{ $department->is_active ? 'Desactivar' : 'Activar' }}</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $departments->links() }}</div>@endsection
