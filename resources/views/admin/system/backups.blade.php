@extends('layouts.admin') @section('title','Backups') @section('header','Backups') @section('content')<div class="flex justify-between"><h1 class="text-2xl font-bold">Backups privados</h1><form method="POST" action="{{ route('admin.system.backups.store') }}">@csrf<button class="rounded bg-red-600 px-4 py-3">Crear backup</button></form></div><ul class="mt-6 space-y-2">@forelse($files as $file)<li class="flex justify-between rounded border border-white/10 p-3"><span>{{ $file }}</span>@if(!str_ends_with($file,'.manifest.json'))<form method="POST" action="{{ route('admin.system.backups.verify',urlencode($file)) }}">@csrf<button class="underline">Verificar</button></form>@endif</li>@empty<li class="text-gray-400">No hay backups.</li>@endforelse</ul>@endsection
