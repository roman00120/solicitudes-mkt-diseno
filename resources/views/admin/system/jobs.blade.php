@extends('layouts.admin') @section('title','Jobs fallidos') @section('header','Jobs fallidos') @section('content')<h1 class="text-2xl font-bold">Jobs fallidos</h1><div class="mt-6 overflow-x-auto rounded border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/5"><tr><th class="p-3">UUID</th><th class="p-3">Queue</th><th class="p-3">Fecha</th><th class="p-3"></th></tr></thead><tbody>@foreach($jobs as $job)<tr class="border-t border-white/10"><td class="p-3">{{ $job->uuid }}</td><td class="p-3">{{ $job->queue }}</td><td class="p-3">{{ $job->failed_at }}</td><td class="p-3"><form method="POST" action="{{ route('admin.system.jobs.retry',$job->uuid) }}">@csrf<button class="underline">Reintentar</button></form></td></tr>@endforeach</tbody></table></div>{{ $jobs->links() }}@endsection
