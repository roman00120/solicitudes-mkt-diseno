@extends('layouts.admin') @section('title','Configuración') @section('header','Configuración')
@section('content')<h1 class="text-2xl font-bold">Configuración del sistema</h1><p class="mt-2 text-sm text-gray-400">Solo se muestran claves operativas permitidas. No almacenes secretos aquí.</p><form method="POST" action="{{ route('admin.settings.update') }}" class="mt-6 max-w-2xl space-y-4 rounded border border-white/10 bg-[var(--color-bg-secondary)] p-5">@csrf @method('PATCH')@foreach($settings as $key=>$value)<label class="block text-sm">{{ $key }}<input name="{{ $key }}" value="{{ $value }}" class="mt-1 w-full rounded bg-black/20 px-3 py-3"></label>@endforeach<button class="rounded bg-red-600 px-4 py-3">Guardar configuración</button></form>@endsection
