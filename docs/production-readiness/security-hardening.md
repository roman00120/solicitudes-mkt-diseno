# Hardening

Se aplican CSRF, auth/active, roles, policies, cookies configurables, CSP, cabeceras, rate limiting, storage privado, auditoría y redacción de secretos. Producción exige HTTPS, `APP_DEBUG=false` y `SESSION_SECURE_COOKIE=true`.
