# Runbook

Diagnóstico: `app:validate-environment --strict`, `app:verify-database`, `app:smoke-test`, `queue:failed`, `schedule:list`. Para incidentes activar mantenimiento, preservar evidencia, verificar backups y aplicar rollback documentado.
