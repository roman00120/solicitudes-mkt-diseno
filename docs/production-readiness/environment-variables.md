# Variables

`.env.example` documenta aplicación, base de datos, sesión, cache, queue, filesystem privado, backups, retención, mail y CSP. Los valores secretos se inyectan fuera del repositorio y nunca se copian a documentación o logs.
