# Restore

Primero usar `app:restore <backup> --dry-run`. Restauración real se ejecuta únicamente por CLI, con ventana de mantenimiento, backup previo y confirmación. MySQL se restaura con el cliente `mysql` tras verificar checksum.
