# Modelo de datos

Un entregable pertenece a una solicitud y tiene versiones. Una versión tiene archivos y revisiones. Las correcciones apuntan a la versión que las originó y pueden resolverse con una versión posterior. `current_version_id` y `approved_version_id` identifican versiones sin copiar archivos.
