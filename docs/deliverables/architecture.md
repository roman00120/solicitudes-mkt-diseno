# Arquitectura

Las entidades principales son `Deliverable`, `DeliverableVersion`, `DeliverableFile`, `DeliverableReview` y `CorrectionRequest`. Los servicios de `App\Services\Deliverables` encapsulan cada etapa; `creative_request_events` solo registra actividad resumida.
