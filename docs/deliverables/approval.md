# Aprobación

Marketing solo puede aprobar la versión actual en `marketing_review`, con confirmación explícita y sin correcciones abiertas. La revisión es inmutable, se establece `approved_version_id` y los archivos quedan protegidos.
