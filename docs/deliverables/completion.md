# Finalización

Supervisor finaliza mediante POST solo cuando la solicitud y entregable están aprobados y existe archivo final o principal. Se cambian ambos estados a `completed` y se registra `request_completed`.
