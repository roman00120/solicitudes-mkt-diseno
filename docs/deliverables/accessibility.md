# Accesibilidad

Las vistas usan landmarks del layout, headings, labels, tablas/listas semánticas, foco visible y acciones de formulario accesibles. La aprobación y corrección son acciones separadas y no dependen solo del color.
