# Testing

La suite cubre creación, archivos privados, revisión interna, envío a Marketing, aprobación, correcciones y nueva versión. Ejecutar `php artisan test`, `npm.cmd run build`, `vendor/bin/pint --test`, `composer validate` y `composer audit`.
