# Archivos

Los binarios viven en el disco privado `local`, con nombre almacenado generado, metadata, checksum SHA-256 y categoría. Se validan extensión, tamaño y servicio. La configuración local permite conceptualmente 100 MB; el límite efectivo depende de `upload_max_filesize`/`post_max_size`.
