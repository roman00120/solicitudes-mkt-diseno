# Información solicitada

Se usa `request_information_requests` con estado open/answered/closed. El mensaje admite 2,000 caracteres. El estado pasa a waiting_for_information y Marketing responde una sola solicitud abierta; luego vuelve a in_validation o assigned según el estado anterior.
