# Accesibilidad

Se usan skip link, landmarks, headings, tablas semánticas, labels, foco visible, links y formularios navegables por teclado. El Kanban ofrece enlaces de detalle y formularios alternativos; no depende de drag and drop ni solo del color.
