# Modelo

`creative_requests` conserva `requested_priority` y `required_date` como valores de Marketing. Fase 7 agrega `operational_priority`, asignación, validación, `internal_due_date`, timestamps operativos y eventos. `request_information_requests` evita almacenar el flujo crítico en comentarios genéricos.
