# Arquitectura

`CreativeRequestQuery` concentra alcance por rol/servicio, filtros y eager loading. Los servicios `RequestTransitionService`, `RequestValidationService`, `RequestAssignmentService`, `RequestInformationService` y `RequestOperationalService` contienen reglas transaccionales. Blade solo presenta datos.
