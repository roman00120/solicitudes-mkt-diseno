# Testing

Las pruebas cubren roles, alcance por servicio, validación, asignación, rechazo, transiciones e información. Ejecutar `php artisan test`, `vendor/bin/pint --test`, `composer validate`, `composer audit` y `npm.cmd run build`.
