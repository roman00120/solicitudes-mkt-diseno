# Testing

La suite cubre regresiones de autenticación, roles, Wizard y Dashboard. Para Fase 6 se deben cubrir índice propio, filtros, detalle, descarga, duplicación y cancelación; las verificaciones de entrega son `php artisan test`, `vendor/bin/pint --test`, `composer validate`, `composer audit` y `npm.cmd run build`.
