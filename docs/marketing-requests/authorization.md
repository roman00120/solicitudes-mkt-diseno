# Autorización

`CreativeRequestPolicy` define `viewAny`, `view`, `create`, `update`, `duplicate`, `cancel`, `downloadFile` y `resumeDraft`. Marketing queda limitado por `requester_id`; Admin y Supervisor pueden consultar conforme al alcance existente, sin obtener acciones operativas adicionales.
