# Accesibilidad

Se usan landmarks del layout, un H1, tabla semántica con caption y scope, labels, enlaces de teclado, foco visible del Design System, modal con `role=dialog`, `aria-modal`, cierre con Escape y texto de estado independiente del color.
