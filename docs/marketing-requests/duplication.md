# Duplicación

La duplicación filtra explícitamente campos generales y detalles, genera UUID y folio nuevos, conserva al solicitante actual, crea estado `draft` y registra `request_duplicated`. No copia archivos, eventos anteriores ni estados operativos.
