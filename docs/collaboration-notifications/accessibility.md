# Accesibilidad

Se incluyen landmarks, encabezados, labels, focus visible, enlaces de ancla `#comment-{uuid}`, fechas exactas en `datetime`, estados textuales y distinción de notas internas más allá del color.
