# Rutas

Todas las rutas requieren autenticación y usuario activo. Administración usa `role:admin`; supervisión `role:supervisor`; métricas personales restringen al propietario. Exportaciones tienen límites por hora y las escrituras usan POST/PATCH/DELETE.
