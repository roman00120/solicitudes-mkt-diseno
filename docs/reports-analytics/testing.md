# Tests

`tests/Feature/Reports/ReportsTest.php` valida alcance por rol, exclusión de borradores, ciclo inválido y exportaciones. Ejecutar `php artisan test`, `vendor/bin/pint --test` y `npm.cmd run build`.
