# Definiciones

Cada métrica declara periodo y universo. `enviadas` exige `submitted_at`; `activas` excluye draft/completed/cancelled/rejected; `vencidas` compara `required_date` con la fecha local; `ciclo` exige fechas válidas. Porcentajes sin denominador cero se muestran como sin base.
