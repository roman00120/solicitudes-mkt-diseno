# Tiempo por estado

`request_status_periods` conserva inicio, cierre y duración. `reports:rebuild-status-periods --dry-run` reconstruye desde eventos sin modificar datos; los huecos históricos se marcan como limitación.
