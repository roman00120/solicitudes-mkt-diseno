# Validación

Comandos de Fase 3:

```powershell
php artisan migrate
php artisan db:seed
php artisan route:list
php artisan test
composer validate
composer audit
vendor/bin/pint --test
npm.cmd run build
```

La suite cubre login, rate limiting, logout, estados, roles, redirecciones, recuperación, reset, confirmación, ausencia de registro público y protección del Design System.

